# Handoff 26051401

> last updated: 2026-05-14 09:27 KST
> status: historical snapshot
> superseded by: [handoff_26051801.md](/Users/bhkim/Documents/codex_prj_sam_asset/세션/handoff_26051801.md)
> previous active handoff: [handoff_26051101.md](/Users/bhkim/Documents/codex_prj_sam_asset/세션/handoff_26051101.md)

이 문서는 2026-05-14 기준 qwen tool/function call 테스트 작업을 이어가기 위해 작성했던 handoff의 historical snapshot이다. 2026-05-18 이후 새 세션 시작 기준은 `handoff_26051801.md`다. 2026-05-11 handoff는 qwen visual OCR, 동양생명 parser-first 실험, 이전 기준선 설명으로 보존한다.

## 1. 현재 작업 주제

- Novita endpoint에서 qwen 계열 모델의 tool/function call 동작을 검증했다.
- 비교 대상 모델:
  - `qwen/qwen3.5-397b-a17b`
  - `qwen/qwen3.6-27b`
- 테스트 범위:
  - 신형 `tools/tool_calls`
  - legacy `functions/function_call`
  - tool result loop
  - 고객/계좌정보 마스킹
  - negative prompt에서 원문 재노출 방지
  - artifact redaction scan
  - ReadTimeout/429/5xx retry 처리
- production `app/` 코드는 변경하지 않았다. 테스트 harness와 문서/보고서 중심 작업이다.

## 2. 최신 종합 보고서

- 최신 단독 제출용 보고서:
  - [novita_qwen35_qwen36_readtimeout_retry_all_12runs_161919.md](/Users/bhkim/Documents/codex_prj_sam_asset/test_report/20260513/novita_qwen35_qwen36_readtimeout_retry_all_12runs_161919.md)
- 보고서 정리 상태:
  - 단독 제출용으로 다른 문서 링크와 개별 report root 링크를 제거했다.
  - 긴 `시나리오별 성공/실패, LLM 처리시간, Token Usage` 상세 섹션을 삭제했다.
  - `4. 테스트 시나리오 설명`은 처음 보는 사용자도 이해할 수 있도록 쉬운 문장으로 다시 작성했다.
  - `2.1 LLM 호출 파라미터`를 추가했다.
  - TC05 result loop 설명을 1차 도구 호출, tool 결과 재주입, 2차 최종 답변 흐름으로 풀어썼다.
  - 마스킹 케이스의 실제 출력값이 보고서 저장 전 redaction을 거친 placeholder 표시값이라는 설명을 추가했다.

## 3. 최종 테스트 결과 요약

- 전체 12run verdict:
  - `PASS 10`
  - `FAIL 2`
  - `PARTIAL 0`
  - `BLOCKED 0`
- 모델별 결과:
  - `qwen3.5-397b-a17b`: 6run 기준 `PASS 5 / FAIL 1`
  - `qwen3.6-27b`: 6run 기준 `PASS 5 / FAIL 1`
- retry/timeout 결과:
  - ReadTimeout/429/5xx retry 로직은 harness에 적용되어 있다.
  - 이번 12run에서는 retry 발생 `0`이다.
  - provider timeout 지속으로 인한 FAIL은 없었다.
- artifact redaction:
  - artifact leak `0`
  - 실제 API key, Authorization, 실제 고객정보, 실제 계좌정보는 보고서에 포함하지 않았다.

## 4. FAIL 원인

### qwen3.5 rerun cycle02

- 실패 case: `TC15_masking_tool_result_loop`
- 모델: `qwen/qwen3.5-397b-a17b`
- 현상:
  - 마스킹 tool call 자체는 생성했다.
  - 하지만 `text` argument에서 `고객명`, `주민번호`, `생년월일`, `계좌번호` 같은 필드 라벨이 빠졌다.
  - 기대값은 필드 라벨을 포함한 전체 synthetic 본문 exact match였다.
- 판정:
  - HTTP 200, final answer 마스킹 형식은 정상.
  - 1차 tool argument exact match 실패로 `FAIL`.
- 의미:
  - timeout 문제가 아니라 argument boundary/field label 보존 문제다.

### qwen3.6 initial cycle01

- 실패 case: `TC10_thinking_compat`
- 모델: `qwen/qwen3.6-27b`
- 현상:
  - `enable_thinking=true`에서 정상 `get_weather` tool call과 함께 빈 tool call이 하나 더 생성됐다.
  - 빈 tool call은 `name=null`, `arguments={}` 형태였고 arguments parse가 실패했다.
- 판정:
  - HTTP 200이고 기대 tool call도 포함되어 있었다.
  - 하지만 빈 tool call 때문에 protocol validation이 실패해 `FAIL`.
- 의미:
  - timeout 문제가 아니라 thinking 옵션 사용 시 tool call 배열 품질이 흔들릴 수 있다는 문제다.

## 5. LLM 호출 파라미터

- endpoint: `https://api.novita.ai/openai`
- 호출 방식: OpenAI-compatible chat completions
- 테스트 모델:
  - `qwen/qwen3.5-397b-a17b`
  - `qwen/qwen3.6-27b`
- 공통 live 호출:
  - `temperature=0`
  - `stream=false`
  - 1차 테스트 호출 `max_tokens=4096`
  - `enable_thinking=false`
  - 단, `TC10_thinking_compat`만 `enable_thinking=true`
- preflight 간단 호출:
  - `max_tokens=1024`
- result loop 2차 호출:
  - `TC05_tool_result_loop`: `max_tokens=1024`
  - `TC15_masking_tool_result_loop`, `TC16_negative_original_request`: `max_tokens=4096`
- timeout/retry:
  - 요청 1회 timeout `120초`
  - retry 최대 `3회`
  - retry 대상: ReadTimeout, ConnectTimeout, Timeout, ConnectionError, HTTP 429, HTTP 5xx

## 6. 핵심 코드와 문서 위치

- live harness:
  - [run_qwen36_tool_function_calls.py](/Users/bhkim/Documents/codex_prj_sam_asset/tests/tool/run_qwen36_tool_function_calls.py)
- 네트워크 없는 단위 테스트:
  - [test_qwen36_tool_redaction.py](/Users/bhkim/Documents/codex_prj_sam_asset/tests/tool/test_qwen36_tool_redaction.py)
- 테스트 프로세스 설명:
  - [tool_call_test_process.md](/Users/bhkim/Documents/codex_prj_sam_asset/tests/tool/doc/tool_call_test_process.md)
- tool call 개발 가이드:
  - [08_tool_call_개발_가이드.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/08_tool_call_개발_가이드.md)

## 7. 실행 명령

작업 재개 시 먼저 단위 테스트를 실행한다.

```bash
cd /Users/bhkim/Documents/codex_prj_sam_asset
.venv/bin/python -m unittest tests/tool/test_qwen36_tool_redaction.py -q
```

Novita preflight 예시:

```bash
.venv/bin/python tests/tool/run_qwen36_tool_function_calls.py \
  --base-url https://api.novita.ai/openai \
  --model qwen/qwen3.5-397b-a17b \
  --actual-model-note qwen3.5-397b-a17b \
  --preflight-only
```

qwen3.5 live run 예시:

```bash
RUN_DATE="$(date +%Y%m%d)"
RUN_TIME="$(date +%H%M%S)"
.venv/bin/python tests/tool/run_qwen36_tool_function_calls.py \
  --base-url https://api.novita.ai/openai \
  --model qwen/qwen3.5-397b-a17b \
  --actual-model-note qwen3.5-397b-a17b \
  --report-root "test_report/${RUN_DATE}/qwen35_397b_tool_function_${RUN_TIME}"
```

qwen3.6 live run 예시:

```bash
RUN_DATE="$(date +%Y%m%d)"
RUN_TIME="$(date +%H%M%S)"
.venv/bin/python tests/tool/run_qwen36_tool_function_calls.py \
  --base-url https://api.novita.ai/openai \
  --model qwen/qwen3.6-27b \
  --actual-model-note qwen3.6-27b \
  --report-root "test_report/${RUN_DATE}/qwen36_27b_tool_function_${RUN_TIME}"
```

## 8. 다음 세션 시작 순서

1. 이 handoff를 먼저 읽는다.
2. 최신 종합 보고서를 확인한다.
3. `tests/tool` harness와 단위 테스트를 확인한다.
4. 새 모델이나 새 조건으로 재시험할 경우 단위 테스트를 먼저 실행한다.
5. live test 전 Novita preflight를 수행한다.
6. 재시험 결과가 생성되면 기존 12run 보고서 형식을 재사용하되, 단독 제출용 문서 조건을 유지한다.
7. 민감정보 테스트는 synthetic fixture만 사용한다.

## 9. 남은 과제

- `TC15_masking_tool_result_loop`의 argument boundary를 더 강하게 보장할지 결정한다.
  - 후보: prompt/schema 강화, result loop 전 argument validation 강화, 실패 시 재시도 정책 분리.
- `TC10_thinking_compat`에서 빈 tool call이 섞이는 현상에 대한 guard 또는 평가 정책을 검토한다.
  - 후보: 빈 tool call을 protocol fail로 유지, 빈 tool call filter를 별도 compatibility mode로 추가, thinking mode 운영 비활성 권고.
- 신규 모델 추가 시 현재 12run 비교 형식을 재사용한다.
- 보고서 제출 전에는 아래 항목을 다시 검수한다.
  - 다른 문서 링크 불필요 여부
  - 긴 case별 raw 상세 표 포함 여부
  - API key/Authorization/실제 고객정보/실제 계좌정보 노출 여부
  - 결과 수치와 실패 원인 설명 일치 여부
