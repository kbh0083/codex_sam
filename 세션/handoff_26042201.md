# Handoff 26042201

- last updated: 2026-04-22

## 목적
- 새 세션에서 `로컬 변경 범위 정리 -> WAS selective merge -> focused validation -> 후속 리팩토링`을 바로 이어갈 수 있도록 만든 실행 인계 문서다.
- 과거 handoff는 archive로 통합해 두었고, 현재 작업 판단은 이 문서와 [handoff_archive_pre260422.md](/Users/bhkim/Documents/codex_prj_sam_asset/세션/handoff_archive_pre260422.md)를 기준으로 한다.

## 현재 기준 사실
- 2026-04-22 기준 로컬에는 아래 변경 묶음이 구현 완료된 상태다.
  - deterministic duplicate-copy guard 복구
  - 내부 `reason_code + reason_summary` 도입
  - stage별 `reason_code` parser whitelist
  - `transfer_amount` post-validation reason 보강
  - LLM 순차 실행 고정(`1건씩`)
  - `*_llm_metrics.json` sidecar 추가
  - 동양생명 active-family shortcut 적용
- 2026-04-22 로컬 live exact 기준:
  - `document/동양생명_20260318.html` -> `COMPLETED`, `issues=[]`, `orders=52`, baseline exact same
  - `document/동양생명_20260413.html` -> `COMPLETED`, `issues=[]`, `orders=48`, baseline exact same
  - `output/correct_result/11_카디프__카디프_251127.pdf` -> `COMPLETED`, `base_date=2025-11-27`, `orders=46`, baseline exact same
- targeted live 5건은 현재 `OK`다.
  - cardif duplicate XLSX guard
  - hana legacy PDF guard
  - heungkuk-hanais duplicate PDF guard
  - dongyang `20260318`
  - dongyang `20260413`
- 동양생명 shortcut 적용 이후 live elapsed는 아래로 줄었다.
  - `동양생명_20260318` -> `85.335s`
  - `동양생명_20260413` -> `85.267s`
- sidecar 기준 동양생명 2건 모두 후반 stage가 실제로 실행되지 않았다.
  - `t_day=0`
  - `transfer_amount=0`
- 현재 잔여 지연은 `t_day`가 아니라 `instruction_document`/`fund_inventory`의 transport retry 쪽이다.
- 최신 deterministic/unit/component 기준은 `303 tests OK`다.

## 현재 로컬 코드 상태
- `app/extractor.py`
  - `base_date` 문서 단일 fan-out 경로 유지
  - duplicate-copy precheck와 동양생명 active-family shortcut 반영
  - 내부 `reason_code/reason_summary`, whitelist, `transfer_amount` reason post-validation 반영
  - LLM 순차 실행 및 metrics sidecar 반영
- `app/prompts/extraction_prompts.yaml`
  - `reason_code`, `reason_summary`, retry summary 문맥, stage별 whitelist taxonomy 반영
- `tests/test_counterparty_live_regression.py`
  - guard 3건과 동양생명 sidecar 검증 반영
- `tests/test_service_guard.py`, `tests/test_extractor_logic.py`, `tests/test_component.py`
  - duplicate guard, reason metadata, retry/metrics, 순차 실행 검증 반영
- 현재 이 로컬 변경 묶음은 아직 WAS repo에 selective merge되지 않았다.

## 다음 작업
1. 로컬 변경 범위를 먼저 정리한다.
   - extractor 내부 reason metadata
   - duplicate-copy precheck
   - sequential LLM execution
   - metrics sidecar
   - 동양생명 active-family shortcut
2. 변경 범위를 WAS에 selective merge한다.
   - `parser.py`, `settings.py`, `.env`는 제외한다.
   - duplicate-copy guard의 source of truth는 prompt가 아니라 코드 helper라는 점을 유지한다.
3. WAS focused validation을 수행한다.
   - 최소 기준: guard 3건 + 동양생명 2건
   - 보고서는 [03_테스트_실행_가이드.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/03_테스트_실행_가이드.md) 기준으로 작성한다.
4. merge 후 refactor backlog를 정리한다.

## 운영/병합 주의
- WAS source of truth는 파일이 아니라 DB row다.
  - `tb_variable_counterparties.prompt`
  - `use_counterparty_prompt`
  - `only_pending`
  - `designated_password`
  - `delivery_type`
- 아래 WAS 파일은 명시 지시 없이는 수정하지 않는다.
  - `src/app/common/document/parser.py`
  - `src/app/config/settings.py`
  - `.env`
- `only_pending`은 추출 규칙이 아니라 최종 출력 직전 후처리 계약이다.
- `DocumentLoadTaskPayload` shape와 외부 payload schema는 이번 작업 범위에서 바꾸지 않는다.
- `reason_code + reason_summary`는 내부 메타데이터다.
- `*_llm_metrics.json`은 내부 debug artifact다.

## LLM 터널 확인 절차
- 기본 확인 순서:
  1. `lsof -nP -iTCP:3910 -sTCP:LISTEN`
  2. `curl http://localhost:3910/v1/models`
- 3910 포트가 닫혀 있으면 아래 명령으로 터널을 다시 연다.
  - `ssh -L 3910:localhost:3950 minisoft@1.241.20.229 -p 2194`
- 비밀번호 같은 민감 정보는 repo 문서에 남기지 않는다.

## 다음 세션 시작 순서
1. 이 handoff를 먼저 읽는다.
2. [handoff_archive_pre260422.md](/Users/bhkim/Documents/codex_prj_sam_asset/세션/handoff_archive_pre260422.md)를 읽어 장기 기준선과 운영 제약을 다시 확인한다.
3. [00_시작_안내.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/00_시작_안내.md), [01_시스템_구성_가이드.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/01_시스템_구성_가이드.md), [02_WAS_병합_가이드.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/02_WAS_병합_가이드.md), [03_테스트_실행_가이드.md](/Users/bhkim/Documents/codex_prj_sam_asset/readme/03_테스트_실행_가이드.md)를 읽는다.
4. 로컬 `git status --short --untracked-files=all`로 현재 diff를 확인한다.
5. LLM 터널 상태를 확인한 뒤 merge-first 순서로 로컬 변경 범위를 정리한다.
